find . -type f -name '*.gif' -print | while read -r file; do
    filename="${file##*/}"         # Strip directory path
    name="${filename%.gif}"        # Remove .gif extension
    echo "$name"
done | LC_ALL=C sort -f
